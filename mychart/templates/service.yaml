apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart_project.fullname" . }}
  labels:
    {{- include "chart_project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "chart_project.selectorLabels" . | nindent 4 }}
